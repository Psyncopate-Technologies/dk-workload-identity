# Copy to .env and fill in — .env is gitignored.
# Load into your shell before running terragrunt locally:
#   set -a; source .env; set +a

# ---- Confluent Cloud (Cloud API key with OrganizationAdmin) ----
# Used by both terraform/ (workload identity) and poc-infra/ (PoC platform).
# Dev-org values live at:
#   /Users/ayeleadmassu/Documents/Confluent-PS/DKP/engagment-2/Day-2/credentials/api-keys.env
TF_VAR_confluent_cloud_api_key=""
TF_VAR_confluent_cloud_api_secret=""

# ---- Azure (PoC infra only — poc-infra/ stack) ----
# Tenant is Ayele's dev Entra tenant. Subscription must be one you can provision in.
TF_VAR_azure_tenant_id="1b9dca15-4db4-4905-8725-d318d11c6875"
TF_VAR_azure_subscription_id=""

# Azure auth for local runs: use the interactive CLI session.
#   az login --tenant $TF_VAR_azure_tenant_id
#   az account set --subscription $TF_VAR_azure_subscription_id
